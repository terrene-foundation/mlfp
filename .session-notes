# Session Notes — 2026-07-10

## Where we are

Deck-overflow remediation is COMPLETE and merged to `main` (PRs #10, #11). All 6
module decks pass the overflow gate 0/460 with Inter loaded; D/T/R fixed to
Department/Team/Role; all deck/readings PDFs regenerated and LFS-clean. No deck
work outstanding. Working tree holds only pre-existing non-mine changes.

## Read first

1. `scripts/check-deck-overflow.js` — the SINGLE canonical overflow/clip detector
   (rewritten this session). Run `node scripts/check-deck-overflow.js` to verify
   any deck edit. Do NOT reintroduce a second detector.
2. `specs/redlines.md` (§ overflow audit test) — the BLOCKING criteria the detector
   enforces (EXTENT past frame OR CLIP inside it under overflow:hidden/auto/scroll).
3. `modules/assets/css/theme.css` — `.code-fit` (opt-in code-shrink) is the go-to
   fix for clipped code slides; equation-box font shrink for wide formulas.
4. `CLAUDE.md` — repo identity, two-format + framework-first directives.

## Traps

- Decks are fixed-height flex sections with `overflow:hidden`; overflowing content
  is CLIPPED silently (no visible spill). Always verify edits with the detector,
  then screenshot at native 1280×720 — visual "looks fine" != passes.
- `gh pr merge --admin` on this repo leaves local `main` stale + LFS working-tree
  residue that blocks `git merge --ff-only`. Fix: `git checkout origin/main -- <the
  readings/*.pdf paths>` then `git merge --ff-only origin/main`.
- `modules/**/readings/*.pdf` are Git-LFS tracked; regenerated PDFs must be
  `git add`ed so the LFS filter makes pointers (verify: `git show HEAD:<path>|head`).
- Leftover (out of scope, non-deck): `data/mlfp05/ag_news_test.parquet` is a raw
  blob that `git lfs fsck` flags — one-liner `git add --renormalize` when convenient.
