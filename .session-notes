# Session Notes — 2026-05-06

## Where we are

HIGH-01 (M4 ExperimentTracker) closed for ex_1+ex_2+ex_3+ex_4 on
`feat/m4-engine-first` (PR #2 against main). Branch is at the natural
PR-review boundary. This session added one chore commit on top of that:
journal triage — promoted 6 manual entries to numbered slots, deleted
24 hook-generated curriculum auto-extracts, replaced 30 mlfp06-migration
auto-extracts with one curated migration-summary entry.

## Read first

1. `git log --oneline main..feat/m4-engine-first` — r1-r5 + chores + journal triage
2. `workspaces/curriculum/journal/0002`–`0007` — the 6 promoted manual entries (M5 generator gap, R11–R13 codify, M6 audit, M6 Ollama R14, Kaizen TextDelta trap, kailash-ml 0.12 gap analysis)
3. `workspaces/mlfp06-migration/journal/0001` — 9-shard migration summary (replaces 30 hook auto-extracts)
4. `SWEEP-2026-04-28.md` — outstanding scope; ex_5/6/7/8 are out of HIGH-01 (the SWEEP miscounted M4's lesson count as 5; actual is 8)
5. `CLAUDE.md` — engine-first / two-format / polars-only directives
6. `shared/mlfp04/ex_4.py` — canonical M4 tracker helpers; mirror for ex_5+
7. `modules/mlfp04/solutions/ex_3/01_pca.py` — destination-first close pattern

## In-flight state

- `.pending/` directories cleared in both workspaces (curriculum + mlfp06-migration). Future hook auto-extracts will rebuild them on next SessionEnd that matches a journal-worthy commit.
- `modules/mlfp05/readings/deck.pdf` persistent `M` from pre-existing LFS migration gap (filed as follow-up — needs `git lfs migrate import`).
- `/tmp/smoke_with_timeout.sh` — kill-after-MASTERED watchdog wrapping the shared M4 shutdown hang; recreate per session.

## Traps

- Shared M4 `setup_engines()` orphans an aiosqlite worker thread; every solution hangs at `Py_Finalize` after the REFLECTION print. Logic completes; only shutdown blocks. Filed as a follow-up shard (~26 files; needs sharding plan before authorizing).
- `AnomalyDetectionEngine.detect`, `DimReductionEngine.reduce`, `ClusteringEngine.fit` are all SYNC despite `km.list_engines()` reporting `is_async=True`. Don't `await`.
- Tracker key regex `^[a-zA-Z_][a-zA-Z0-9_.\-]*$` — use `_slug()` for prose keys; encode `0.001 → "0_001"` for sweep keys with dot-digit pairs.
- Tracker rejects NaN/inf via `MetricValueError`; every emit through `_finite()` (silhouette, AUC-ROC, AP can collapse to NaN).
- SQLite contention: never run two M4 trackers against the same `.db` concurrently. ex_1 / ex_3 / ex_4 each have their own store.
- LFS attribute `modules/**/readings/*.pdf` was added after the PDFs were committed inline → eternal `M` until `git lfs migrate import` runs.
- `.venv` is Python 3.13 / kailash-ml 1.5.1; pyenv-global resolves a DIFFERENT kailash_ml. Always `.venv/bin/python`.

## Open questions for the human

1. PR #2 review/merge, or take more shards on this branch?
2. Authorize the LFS migrate (history-rewriting)?
3. Authorize the shared `setup_engines()` shutdown-hang fix? Touches ~26 files; needs sharding plan first per `autonomous-execution.md` MUST Rule 1 (≤500 LOC load-bearing / ≤5–10 invariants per shard).
4. SessionEnd hook emits 2–4 duplicate auto-extracts per commit (see 0001 §Why curated). Worth fixing the hook upstream, or accept that future `/journal` triage will dedupe?
