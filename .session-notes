# Session Notes — 2026-04-05 (Sessions 1-3)

### Read These Files First (in order)

1. `CLAUDE.md` — 6 absolute directives, course structure, module-framework mapping
2. `workspaces/curriculum/briefs/course-brief.md` — Authoritative curriculum (6 modules, 7h each)
3. `workspaces/ascent01/briefs/module-brief.md` through `ascent06` — Per-module briefs with exercises, datasets, quiz topics
4. `workspaces/datasets/briefs/dataset-brief.md` — Dataset curation plan (what to create/source)
5. `.claude/agents/education/kailash-tutor.md` — Verified SDK API patterns (from red team)
6. `.claude/agents/education/exercise-designer.md` — Stripping modes, blank counts, converter reference
7. `scripts/py_to_notebook.py` — Canonical .py→.ipynb converter (--all, --module, single file)

## Accomplished

- **34 solutions** written (9,600 lines) across 6 modules, red-team reviewed, 8 critical SDK API issues fixed
- **102 exercise files** generated: 34 local .py + 34 Jupyter + 34 Colab notebooks with progressive scaffolding (70%→20%)
- **COC sync** from kailash-coc-claude-py v2.6.0 (26 commands, 445 skills, 32 agents, 54 scripts)
- **4 agents codified** with session learnings: exercise-designer (stripping modes), notebook-validator (converter checks), kailash-tutor (6 verified API patterns), dataset-curator (reuse policy, naming)
- **13 commits**, repo at `e3ffdb1`

## Outstanding

- [ ] Create synthetic datasets: `sg_credit_scoring.parquet` (100K), `ecommerce_customers.parquet` (200K), `icu_*.parquet` (60K stays), `ecommerce_experiment.parquet` (500K), `sg_cooling_measures.csv` (verified: 0 data files in modules/)
- [ ] Source public datasets: HDB resale (data.gov.sg), `sg_cpi.csv`, `sg_employment.csv`, `sg_fx_rates.csv`, `sg_taxi_trips.parquet`, `credit_card_fraud.parquet` (Kaggle), `sg_news_corpus.parquet`
- [ ] Upload all datasets to ascent_data Google Drive folder ID `16c3RkGmiwMWbjD7cJKbJx-JRZlgmQdws`
- [ ] Build Reveal.js decks: `decks/ascentN/deck.html` per `workspaces/decks/briefs/deck-brief.md` (verified: 0 deck.html files)
- [ ] Write quizzes: `modules/ascentN/quiz/` — 15 questions each (verified: 0 quiz files)
- [ ] Run `uv lock` and commit lockfile (verified: no uv.lock)
- [ ] Create GitHub repo at terrene-foundation/ascent and push
- [ ] Validate solutions end-to-end (needs kailash packages on PyPI + datasets on Drive)
- [ ] Validate notebook 3-format consistency with notebook-validator agent

### Oversight — Verify Before Starting

- [ ] All 34 solutions exist: `find modules/*/solutions -name "ex_*.py" | wc -l` = 34
- [ ] All 102 exercises exist: `find modules/*/local modules/*/notebooks modules/*/colab -name "ex_*" | wc -l` = 102
- [ ] No pandas: `grep -rl "import pandas" modules/ | wc -l` = 0
- [ ] No stale log_run: `grep -rl "tracker\.log_run" modules/*/solutions/*.py | wc -l` = 0

### Oversight — Verify After Completing

- [ ] All solutions run: `uv run pytest tests/ -v` passes (blocked: needs kailash packages)
- [ ] 3-format consistency: run `/validate-notebooks`
- [ ] No hardcoded keys: `grep -rn "sk-\|gsk_\|hf_" --include="*.py" modules/` = 0
- [ ] Dataset files present: `find .data_cache -type f | wc -l` > 0

## Blockers

- Google Drive access needed for dataset upload to ascent_data folder
- terrene-foundation GitHub org access needed for public repo creation
- Kailash SDK packages must be on PyPI before `uv sync` works
