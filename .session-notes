# Session Notes — 2026-06-23

## Where we are

Full Kailash-stack bump is complete and verified: all M1–M6 solutions run
end-to-end. Last change: bumped kailash-align 0.7.3 + kailash-ml 2.2.2
(upstream fixes for kailash-py#1426), unblocked the 2 M6 fine-tuning
exercises (M6 now 39/39), committed as `e2c4dc58`. Next session: M1 dry
run with the human, including coding-environment setup.

## Read first

1. `CLAUDE.md` — course directives (framework-first, polars-only, two-format, progressive disclosure)
2. `modules/mlfp01/solutions/` — M1 exercises (DataExplorer, PreprocessingPipeline, ModelVisualizer); source of truth
3. `modules/mlfp01/local/` — M1 student scaffolds (~70% provided; what the learner actually opens)
4. `shared/data_loader.py` + `shared/mlfp01/` — VS Code data loading + M1 helpers
5. `scripts/generate_selfcontained_notebook.py` — regenerate Colab notebooks after ANY .py/shared change
6. `specs/_index.md` — curriculum spec manifest; load only the M1-relevant spec

## In-flight state

- PR for `feat/m4-engine-first` → `main` opened/merged this session (see git log).
- esperie-ai (Tailscale 100.71.125.70) has the bumped venv synced under ~/mlfp; Ollama on :11434. Only needed for M5/M6 GPU work — M1 runs locally.

## Traps

- Never hand-edit `.ipynb` under colab-selfcontained{,-solutions}/ — regenerate via the generator, then `scripts/check_notebook_syntax.py`.
- Use `.venv/bin/python`, never bare `python`. Run from repo root so `shared.` imports resolve.
- M1 is local-only; do NOT pull in GPU/Ollama setup for the dry run.

## Open questions for the human

- M1 dry run scope: which specific exercise(s) to walk through first, and what "coding setup" baseline the learner starts from (fresh clone? VS Code + uv? Colab?).
