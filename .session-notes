# Session Notes — 2026-04-15

## Where we are

Full-course smoke test COMPLETE on Python 3.13 with kailash 2.8.6 /
kaizen 2.7.4 / nexus 2.0.3. All 160 solution files across M1-M6
import clean. Downstream synced and pushed to pcml-run26/pcml-run26-2601.
Next session: **full specs audit** (`specs/` directory review).

## Read first

1. `specs/_index.md` — entry point for the specs audit
2. `CLAUDE.md` § Course Structure — module-framework mapping
3. `workspaces/mlfp06-migration/api-cheatsheet.md` — canonical modern kaizen/pact/nexus imports (verified against 2.7.4/2.0.3)
4. `git log --oneline 879181e..HEAD` — all commits from the two-session smoke-test pass

## In-flight state

- GitHub org moved from `terrene-foundation` to `pcml-run26` — memory updated, downstream remote confirmed at `pcml-run26/pcml-run26-2601`
- M3 ex_7/{03,05} local/ scaffolds have proper ~50% TODOs but were hand-stripped, not exercise-designer generated. May drift from sibling style.
- `optuna>=3.6`, `lime>=0.2`, `hdbscan>=0.8.33`, `umap-learn>=0.5`, `bertopic>=0.16` added to pyproject.toml this session as previously-undeclared deps.
- `gymnasium[box2d]` extra dropped (unused, no 3.14 wheel).
- Runtime pollution still in tree: `data/mlfp06/`, `mlfp02_experiments.db-{wal,shm}`, `modules/mlfp02/notebooks/`, `modules/mlfp03/notebooks/`. Don't `git add -A`.

## Traps

- **Python 3.14 blocked**: kaizen `Signature` metaclass can't find `InputField`/`OutputField` class attributes on 3.14 (PEP 649 deferred annotations). `_signature_inputs` stays empty. Blocks ALL Signature-based exercises. kailash-py dev is working on a fix. Stay on 3.13.
- **Worktree isolation unreliable**: 3/4 parallel agents in the prior session and 1/1 exercise-designer agent this session wrote to main instead of their worktrees. Plan for serial commits when dispatching specialists.
- **DelegateEvent 0.9 is a typed union**: base class has only `event_type`+`timestamp`. Use `getattr(event, "text", None)` pattern, not `event.text`. Cost via `delegate.consumed_usd` after stream ends.
- **ReActAgent 0.9 constructor reshape**: no `tools=` or budget kwargs. Post-construct: `agent.config.budget_limit_usd = X` + `agent.available_tools.append(tool)`.

## Open questions for the human

- Specs audit scope: full `specs/` rewrite to match post-migration API reality, or targeted review of files that reference stale kaizen/pact patterns?
