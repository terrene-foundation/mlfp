# Session Notes — 2026-04-13 (M5 deep overhaul + R9 established)

## Where we are

All 6 modules expanded to 25h depth. M5 deck overhauled (KaTeX fix, 20 SVG diagrams, 8 application slides, overflow check codified). M5 ex_1 rewritten as R9 reference implementation (10 AE variants + 7 application scripts). Redline 9 ("Seeing is Believing") codified. All changes uncommitted. Next: audit remaining 47 exercises against R9, then M5 quizzes using DIS as baseline.

## Read first

1. `specs/redlines.md` — now 9 redlines. R9 (visual intuition + real-world application) is the new standard. Read R9 fully before touching any exercise.
2. `modules/mlfp05/solutions/ex_1.py` — the R9 reference implementation (10 AE variants, reconstruction grids, latent traversal). All other exercises must match this pattern.
3. `modules/mlfp05/solutions/ex_1_app_*.py` — 7 standalone application scripts (fraud, medical, denoising, timeseries, generation, compression, manufacturing). This is the application script pattern for R9B.
4. `scripts/check-deck-overflow.js` — new puppeteer-based visual overflow detector, wired into `redline-check.py`. Run before any deck change.
5. `modules/mlfp05/deck.html` — KaTeX double-render fixed (idempotent `mlfpRenderMath`), 20 SVGs + 8 app slides integrated. Same KaTeX bug exists in M3/M6 decks (not yet fixed).

## In-flight state

- ALL changes uncommitted — solutions (6 modules), local exercises (6 modules), colab notebooks (M5), exams (6), deck overhaul (M5), CSS (theme.css), scripts (overflow checker, redline-check), specs (redlines R9), pyproject.toml (pyright config, python 3.13, uv overrides), learner fixes (async intro M3, Bayesian intuition M2, EM hint M4 exam).
- M5 local exercises use modern `async with tracker.run() as ctx` API. Other modules do NOT use ExperimentTracker.start_run so no migration needed there.
- M5 ex_1 local/colab NOT yet regenerated from the new 1,919-line solution. Must strip to 30% scaffolding + regenerate colab.
- `.vscode/settings.json` created (gitignored) pointing Pylance to `.venv`. Pyright config added to `pyproject.toml`.

## Traps

- `.venv/bin/python` hangs if stale `mlfp05_*.db-shm`/`.db-wal` files exist from interrupted runs. Clean with `rm -f mlfp05_*.db*` before running solutions.
- `scripts/py_to_notebook.py` creates `modules/mlfpNN/notebooks/` dir (non-standard). Must `rm -rf` after every run — only `local/` and `colab/` are in the two-format spec.
- KaTeX double-render bug exists in M3 and M6 decks (same root cause as M5, not yet ported). Port `mlfpRenderMath` function or lift to `modules/assets/js/`.
- DIS reference materials at `~/Library/CloudStorage/GoogleDrive-jack@integrum.global/.shortcut-targets-by-id/.../PCML_DIS_R5_2601/` — MUST check before writing exercises or quizzes. They set the visualization quality bar.

## Open questions for the human

- R9 audit of remaining 47 exercises: parallel (all modules at once) or sequential (finish M5 first as gold standard, then M1-M4, M6)?
- DIS-style quizzes: still needed after R9 exercise rewrite, or does the application-script pattern replace quizzes?
