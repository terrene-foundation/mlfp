# Sweep — MLFP — 2026-06-22

Repo-wide outstanding-work audit (terrene-foundation/mlfp), branch `feat/m4-engine-first`.
Run after the loom 2.45.0 sync + M1–M4 runtime-fix session.

## Headline

- **M1–M4 solutions: 142/142 pass** (certified by full clean re-run this session).
- **0 open GitHub issues.** **0 stub markers** in production code.
- Working tree clean except 2 pre-existing edits.
- Main outstanding items are **delivery** (22 unpushed commits, 2 open PRs) and
  **untested-in-this-environment M5/M6** (GPU/Ollama), not correctness defects.

---

## Findings

### [HIGH] [Sweep 4] 22 commits unpushed; PR #2 reflects stale branch state

- **Location:** `feat/m4-engine-first` — 22 ahead / 0 behind `origin/main`. PR #2 (`feat/mlfp04: tracker + destination-first close on ex_1`) is open but predates this session's 5 commits.
- **Disposition:** SURFACE — human call (push/merge is a delivery decision).
- **Evidence:** `git rev-list --left-right --count origin/main...HEAD` → `0  22`.
- **Why:** All of this session's work (loom 2.45.0 sync, COC purge, M1–M4 API + pre-existing fixes) lives only locally. Downstream (the GitHub Classroom student repo) sees nothing until this branch merges and `build-student-repo.sh --sync` runs.
- **Action:** none taken (per /sweep: agent does not decide delivery).

### [HIGH] [Sweep 4] PR #3 is a stale, superseded sync

- **Location:** PR #3 `chore/sync-loom-2.19.0` — "adopt loom 2.19.0 + bump SDK pin floors".
- **Disposition:** SURFACE → recommend CLOSE. This session synced to **loom 2.45.0** (PR-#3's loom 2.19.0 is 26 versions behind) and floored pins at PyPI tips well above PR #3's.
- **Why:** Merging PR #3 would regress the stack. It is fully superseded.
- **Action:** none taken (closing another branch's PR is a human call).

### [MED] [Sweep 5/process] M5 + M6 solutions not runtime-certified in this environment

- **Location:** `modules/mlfp05/solutions/**` (51), `modules/mlfp06/solutions/**` (47).
- **Disposition:** DEFER-WITH-REASON (environment-blocked, not a code defect).
- **Evidence:** all M5/M6 solutions **compile + imports resolve** against the new stack; a static drift scan found **0** instances of the M1–M4 drift signatures (`feature_distribution`, `scatter_plot`, `histogram+x_label`, `cum_mean`, `MCPTool`) in M5/M6 solutions. M5 needs GPU/long CPU training; M6 needs Ollama + API keys (`.env` has `OLLAMA_BASE_URL`/`OPENAI_API_KEY`/etc. blank).
- **Why:** "Imports resolve" is a floor, not proof of end-to-end correctness (the M1–M4 cascade proved runtime surfaces bugs static checks miss). M5/M6 cannot be run here.
- **Action:** none — requires a GPU/Ollama environment + API keys.

### [MED] [Sweep 5] M2 local-scaffold histogram drift — FIXED inline

- **Location:** `modules/mlfp02/local/ex_2/04_mle_failures.py`, `modules/mlfp02/local/ex_3/04_permutation_test.py` (3 `viz.histogram(array, …, x_label=…)` calls).
- **Disposition:** FIX-NOW (Rule 1). Migrated to the 2.x API (`pl.DataFrame` + `column=` + `bins=`, dropped `x_label`) mirroring the already-correct solutions; added `import polars as pl`; regenerated both Colab notebooks.
- **Evidence:** post-fix AST re-scan of all locals → `0` remaining `histogram+x_label`. `py_compile` clean. 402 notebooks parse clean.
- **Why:** these student scaffolds would crash in Colab on the new stack even though their solutions pass; they are the last of the known viz-drift class.

### [LOW] [Sweep 6] Two-format consistency: shared-helper ripple into notebooks

- **Location:** `shared/data_loader.py` (fast-fail) and `shared/mlfp03/ex_1.py` (`print_ranking` NaN-safe) changed this session; both are inlined into many notebooks' Cell 1 by the generator.
- **Disposition:** DEFER-WITH-REASON (no runtime impact). The `data_loader` change only affects the missing-file path (Colab uses the Drive-mount branch, not the local-data branch); the `print_ranking` change only affects the NaN case. I regenerated the directly-affected exercises' notebooks; other notebooks carry a slightly older inlined copy of these helpers but are functionally identical for their content.
- **Why:** strict two-format consistency would regenerate every notebook inlining a changed shared helper; functional impact is nil, so deferred rather than mass-regenerating ~400 notebooks.

### [LOW] [Sweep 1/2/5] No workspace todos / pending journals / per-workspace specs

- **Location:** `workspaces/{ascent01-06,assessment,curriculum,datasets,decks,mlfp06-migration}`.
- **Disposition:** FALSE-POSITIVE for this repo's layout. No `todos/active/`, `journal/.pending/`, or per-workspace `specs/` exist; the authoritative curriculum specs live at repo-root `specs/` (per CLAUDE.md), outside the workspace-scoped redteam sweep.
- **Why:** the /sweep template assumes a BUILD-repo workspace layout; MLFP uses a flatter content layout.

### [INFO] [Sweep 6/7] Hygiene clean

- No orphan worktrees (single main checkout at HEAD).
- No stale `.session-notes` >30d under `workspaces/`.
- Working tree: only `.claude/settings.json` + `.session-notes` (pre-existing, untouched this session).
- Stub scan (`NotImplementedError`/`FIXME`/`HACK`/`XXX`) in `modules/` + `shared/`: **1 hit, a comment** explaining an Apple-Silicon MPS limitation in `mlfp05/solutions/ex_4/02_transformer_encoder.py` — not a stub. Clean.

---

## Cross-cutting observations

1. **Running ≠ importing.** The M1–M4 pass rate looked like 100% under compile+import checks, but actually executing surfaced 9 failures (5 stack-drift, 4 pre-existing/flaky). M5/M6 are currently only at the import floor.
2. **The data is realistic, the exercises drifted.** Two "data" failures (ex_2 BISHAN band, ex_4 case-join) were exercises encoding outdated assumptions, not bad data. Fixes corrected the exercises, not the data.
3. **Latent-bug cascades.** ex_4 had 4 bugs stacked behind one crash; each fix exposed the next. Worth assuming other untested exercises (M5/M6) hide similar cascades.

## Recommended next-session items (ranked)

1. **Push `feat/m4-engine-first` and update/merge PR #2**, then **close PR #3** (superseded). Unblocks the downstream student-repo sync.
2. **Run M5/M6 solutions in a GPU + Ollama environment** with `.env` keys populated; expect latent cascades like M1–M4. (98 solutions untested.)
3. **After merge, run `build-student-repo.sh --sync`** to propagate the fixed materials to the GitHub Classroom template.
4. (Optional) Mass-regenerate notebooks if strict shared-helper two-format parity is desired (low value).
