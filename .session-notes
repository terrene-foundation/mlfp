# Session Notes — 2026-04-29

## Where we are

M5 notebook smoke-testing complete: every notebook under
`modules/mlfp05/colab-selfcontained-solutions/` now starts training cleanly
under kailash-ml 1.5.1 (verify via `/tmp/mlfp-smoke/parallel-runner.py
--workers 4`). Branch `fix/expt-tracker-1.1.1-migration` carries the source
fixes; many files uncommitted. Next: commit + push + classroom sync.

## Read first

1. `/tmp/mlfp-smoke/parallel-results.json` — last verified state of all M5
   solution notebooks (PASS-FULL / PASS-WALL / PASS-CELLCAP).
2. `/tmp/mlfp-smoke/parallel-runner.py` — the smoke runner; re-run if anything
   changes. 4 workers, 90s wall, 80s cell, timeout==PASS.
3. `SWEEP-2026-04-28.md` (repo root) — engine-first / seamless-pipeline /
   diagnostics / automl audit. Findings still apply for M1-M4 + M6.
4. `CLAUDE.md` — repo directives (engine-first, polars-only, two-format).
5. Memory `project_downstream_repo.md` — classroom repo URL is
   `pcml-run26/pcml-run26-professional-certificate-in-machine-learning-pcml-run26-2601`
   (NOT terrene-foundation; updated this session).

## In-flight state

- Many files modified across `shared/mlfp05/`, `modules/mlfp05/{solutions,local}/`,
  `modules/mlfp05/colab-selfcontained{,-solutions}/`, `scripts/generate_*.py`.
  Nothing committed yet. Branch is many commits ahead of `main`.
- 3 upstream issues filed at `terrene-foundation/kailash-py`: **#699** (schema
  conflict), **#700** (InferenceServer hard break), **#701** (diagnose() API).
  Each issue body documents the MLFP-side workaround already in this branch.
- `/tmp/pcml-run26-template/` exists as raw files (no .git). For classroom
  rollout, clone fresh: `gh repo clone pcml-run26/pcml-run26-professional-certificate-in-machine-learning-pcml-run26-2601 /tmp/pcml-classroom`.

## Traps

- `shared/mlfp05/*.py::setup_engines` now uses **separate `*_registry.db`** for
  ModelRegistry vs `*.db` for ExperimentTracker (kailash-py#699 workaround).
  Reverting to a single DB will reintroduce the schema-conflict crash.
- `benchmark_inference` in `modules/mlfp05/solutions/ex_3/03_gru.py` infers
  `n_features` from `model.gru.input_size` (sensor models use input_dim=1,
  stock models use 4). Don't refactor it back to the global `N_FEATURES`.
- `km.diagnose(kind="dl", data=val_loader)` silently ignores `data=` per
  kailash-py#701 — DataLoader is not consumed by DLDiagnostics. Calls are
  pedagogical "report exists" markers, not live evaluations.
- Local notebooks under `modules/mlfp05/colab-selfcontained/` intentionally
  keep `____` scaffolding. Generator syntax check fails on those by design.
- `scripts/generate_selfcontained_notebook.py` was patched: only **top-level**
  (column-0) `asyncio.run(...)` is converted to `await ...`. Sync wrappers
  inside `def` bodies must keep `asyncio.run`.

## Open questions for the human

1. Commit strategy: one atomic commit or split by fix-class (schema /
   diagnose / asserts / generator)?
2. Classroom rollout now, or stage on a side branch first?
3. Audit M1–M4 + M6 next? `SWEEP-2026-04-28.md` flagged M3/M4 engine-first
   gaps as the top follow-on.
