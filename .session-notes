# Session Notes — 2026-05-06

## Where we are

Two parallel branches today:

1. `chore/sync-loom-2.19.0` (PR #3) — adopted loom 2.19.0 stack, bumped
   SDK pin floors to template's `sdk_packages` map, purged 7 obsoleted
   paths, added skill `32-trust-posture/` + 6 new rules + 5 new hooks.
   `uv sync` clean; 22 hooks resolve. PR open against main.

2. `feat/m4-engine-first` (PR #2 base) — extended HIGH-01's engine-first
   pattern from ex_1–ex_4 onto **ex_5 (association rules, 4 sub-files)**
   as r6. All 4 solutions reach MASTERED via the watchdog wrapper.
   ExperimentTracker store `m4_assoc_rules_zoo` accumulated 4 runs / 219
   metric rows across apriori / fp_growth / association_rules /
   rule_features. r6 committed (a2ac42a4) on top of HIGH-01 r1–r5.

## Read first

1. `git log --oneline main..feat/m4-engine-first` — r1–r6 + chores
2. `shared/mlfp04/ex_5.py` — canonical helpers; `_slug()` is new
   (handles prose keys + dot-digit sweep names)
3. `modules/mlfp04/solutions/ex_5/01_apriori_from_scratch.py` — reference
   destination-first close where kailash-ml has no engine: pivot to
   mlxtend (the de facto production library)
4. `SWEEP-2026-04-28.md` — outstanding scope; ex_6, ex_7, ex_8 remain
   (5+5+5 sub-files) for r7/r8/r9
5. `CLAUDE.md` — engine-first / two-format / polars-only directives
6. `.claude/VERSION` — now at template 3.9.0 / loom 2.19.0
7. `.session-notes` (this file)

## In-flight state

- PR #2 (HIGH-01 + r6) and PR #3 (sync) both open; neither reviewed.
- `.pending/` directories in workspaces still cleared.
- `modules/mlfp05/readings/deck.pdf` — pre-existing LFS migration trap;
  on `feat/m4-engine-first` the file is an LFS pointer, on `main` /
  `chore/sync-loom-2.19.0` it's an inline blob. To switch branches, may
  need to `mv` the on-disk file out of the way (then restore via LFS
  smudge or just discard — content is identical).
- `/tmp/smoke_with_timeout.sh` — kill-after-MASTERED watchdog; recreate
  per session.
- `/tmp/deck.pdf.real-blob.backup` — created during chore→feat switch,
  cleaned up.

## Traps

- Shared M4 `setup_engines()` orphans an aiosqlite worker thread; every
  solution hangs at `Py_Finalize` after the REFLECTION print. Logic
  completes; only shutdown blocks. Filed as a follow-up shard (~26
  files; needs sharding plan before authorizing).
- `AnomalyDetectionEngine.detect`, `DimReductionEngine.reduce`,
  `ClusteringEngine.fit` are all SYNC despite `km.list_engines()`
  reporting `is_async=True`. Don't `await`.
- Tracker key regex `^[a-zA-Z_][a-zA-Z0-9_.\-]*$` — `_slug()` handles
  prose keys; explicit encoding for `0.001 → "0_001"` sweep keys with
  dot-digit pairs.
- Tracker rejects NaN/inf via `MetricValueError`; every emit through
  `_finite()` (lift/conviction can collapse to NaN/inf at edges).
- SQLite contention: never run two M4 trackers against the same `.db`
  concurrently. ex_1 / ex_3 / ex_4 / **ex_5** each have their own store.
- LFS: see deck.pdf trap above.
- `.venv` is Python 3.13 / kailash-ml **1.7.1** (post-/sync); pyenv-global
  resolves a different kailash_ml. Always `.venv/bin/python`.
- Branch switching with deck.pdf: `git checkout` refuses when LFS
  pointer ↔ inline blob differs. Workaround: `mv modules/mlfp05/readings/
  deck.pdf /tmp/...` before checkout, then either restore via `git
  checkout --` or let LFS smudge recreate it.
- ex_5 has no kailash-ml engine — destination-first close points at
  mlxtend (course dep) + FeatureEngineer/TrainingPipeline for ex_5/04.
  Future ex_6/7/8 will likely have the same gap.

## Open questions for the human

1. Merge PR #3 (sync) first or PR #2 (HIGH-01 + r6) first? PR #3 is
   smaller and more reversible; PR #2 is the substantive curriculum
   delivery.
2. Authorize r7 (ex_6 NLP, 5 sub-files) on this branch? Same pattern,
   but PR will keep growing. Alternative: cut a fresh branch off main
   per exercise to keep PRs focused.
3. Authorize the LFS migrate (history-rewriting)?
4. Authorize the shared `setup_engines()` shutdown-hang fix? Touches
   ~26 files; needs sharding plan first per `autonomous-execution.md`
   MUST Rule 1 (≤500 LOC load-bearing / ≤5–10 invariants per shard).
5. SessionEnd hook emits 2–4 duplicate auto-extracts per commit (see
   journal/0001 §Why curated). Worth fixing the hook upstream, or
   accept that future `/journal` triage will dedupe?
