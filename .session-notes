# Session Notes — 2026-04-14 (M5 + M6 R10 complete, infrastructure solid)

## Where we are

M5 AND M6 fully restructured to R10 directories. Student repo live at `pcml-run26/pcml-run26-2601` (GitHub Classroom template). 16 student repos forked to `pcml-run26` org. Source repo and student repo both pushed.

## R10 status

| Module | Status | Exercises | Technique files |
|--------|--------|-----------|-----------------|
| M1 | monolithic | 8 | 8 (mostly single-technique, may not need R10) |
| M2 | monolithic | 8 | 8 |
| M3 | monolithic | 8 | 8 |
| M4 | monolithic | 8 | 8 |
| M5 | **R10 ✓** | 8 | 42 technique files + 8 shared |
| M6 | **R10 ✓** | 8 | 39 technique files + 8 shared |

## Infrastructure

- `shared/mlfp05/ex_N.py` + `shared/mlfp06/ex_N.py` — installable helpers via `uv sync`
- `scripts/build-student-repo.sh --sync` — incremental rsync to student repo
- `scripts/py_to_notebook.py` — now R10-aware (handles `local/ex_N/` directories)
- `downstream.yaml` — tracks `pcml-run26/pcml-run26-2601`
- Exercise directories: `modules/mlfp0N/{solutions,local,colab}/ex_N/NN_technique.py`

## Repos

| Repo | Purpose |
|------|---------|
| `terrene-foundation/mlfp` | Source (instructor + curriculum) |
| `pcml-run26/pcml-run26-2601` | Student template (GitHub Classroom) |
| `pcml-run26/pcml-run-26-{username}` | Student forks (16 students) |

## Student repo sync status

After the M6 push:
- 5 student repos: "not behind upstream" (already synced from earlier runs)
- 10 student repos: merge conflicts (students had made commits)
- 1 repo: validation failed (esperie test)

Students with conflicts need to manually: `git remote add upstream ...; git fetch upstream; git merge upstream/main`

## Still pending

- **M1-M4 R10 conversion** — M1-M2 are mostly single-technique (progressive tasks, not separate techniques) so may stay as single files. M3-M4 are multi-technique and should be R10-ized.
- **Missing PDFs** — textbooks (M1-M4, M6), notes (all modules), deck (M6)
- **M5/M6 runtime testing** — stripped exercises not yet verified to run correctly when completed
- **exercise-standards.md** — ✓ R10 section added
- **Student repo conflict resolution** — 10 repos need manual merge

## Traps

- `shared` package installable via `pyproject.toml` — `from shared.mlfp06.ex_1 import ...` works from any cwd after `uv sync`
- GitHub template repos cannot have LFS — standard datasets (MNIST, CIFAR, etc.) gitignored
- Student exercises use `from shared.mlfpNN.ex_N import` — no local `_shared.py` or `helpers.py`
- `py_to_notebook.py` now handles R10 directories: `python scripts/py_to_notebook.py modules/mlfp06/local/ex_1/`
- `scripts/build-student-repo.sh --sync` for incremental updates
- Pre-existing Pyright diagnostics for `kailash_ml`, `kailash_align` etc. are expected (they resolve at runtime via uv sync, Pyright just can't find them in its static analysis)

## Recent commits (source)

- `f7f9960` M6 R10 conversion (all 8 exercises)
- `adc5708` M5 shared extraction + R10 refactor
- `11e8953` downstream.yaml → pcml-run26 org
- `f838834` cleanup (screenshots moved to outputs/)
- `d94b91f` downstream infrastructure + readings/
- `25f49b8` remove old monolithic M5 files
- `2998353` Redline 10 + exercise-designer + two-format rule

## Next session priorities

1. M3-M4 R10 conversion (multi-technique exercises)
2. M1-M2 review (decide if R10 needed or single-file is fine)
3. Generate missing PDFs (textbook.pdf, notes.pdf per module)
4. Runtime test M5/M6 stripped exercises
5. Document student upstream-sync workflow (manual merge for conflicts)
